# Школа японского языка — Лендинг

Одностраничный сайт-презентация с акцентом на визуальный вау‑эффект, анимации и конверсию (без LMS функционала пока).

## Стек
- Чистый HTML / CSS / JS (без сборщика на старте)
- Progressive Enhancement (работает без JS: контент доступен)
- Canvas для фоновой анимации лепестков сакуры
- Build pipeline (esbuild + PostCSS autoprefixer/cssnano + хэш ассеты)
- Responsive images (sharp: AVIF/WebP/JPG многократные размеры + манифест)
- Простейший статический блог (Markdown -> HTML)
- Самостоятельно хостим шрифты (Playfair Display, Inter, Noto Sans JP)
- Лёгкая аналитика + очередь событий
- Псевдо-backend для форм (локальная очередь + заглушка API)

## Структура проекта

### 📁 Модульная архитектура

Проект организован в модульную структуру для лучшей поддержки и масштабируемости:

```
q:\japanschool\
├── src/                          # Исходные файлы
│   ├── styles/                   # CSS модули
│   │   ├── base/                 # Базовые стили
│   │   │   ├── variables.css     # CSS переменные и design tokens
│   │   │   ├── themes.css        # Сезонные темы (spring, autumn, winter, sakura)
│   │   │   ├── reset.css         # Глобальные сбросы и base styles
│   │   │   └── layout.css        # Система раскладки (container, grid)
│   │   └── components/           # UI компоненты
│   │       ├── preloader.css     # Загрузочный экран
│   │       ├── header.css        # Шапка с навигацией
│   │       ├── hero.css          # Героя секция
│   │       ├── buttons.css       # Варианты кнопок
│   │       ├── forms.css         # Поля форм и валидация
│   │       ├── faq.css           # FAQ аккордеон (extreme minimalism)
│   │       ├── sections.css      # Секции контента
│   │       ├── reviews.css       # Карусель отзывов
│   │       ├── modal.css         # Модальные окна
│   │       ├── gallery.css       # Лайтбокс галереи
│   │       ├── footer.css        # Футер
│   │       └── utilities.css     # Утилиты (анимации, адаптивность)
│   ├── scripts/                  # JavaScript модули (ES6)
│   │   ├── utils/                # Утилиты
│   │   │   ├── analytics.js      # Система аналитики
│   │   │   └── api.js            # API запросы и offline queue
│   │   ├── components/           # Компоненты UI
│   │   │   ├── theme.js          # Переключение тем (6 вариантов)
│   │   │   ├── preloader.js      # Контроль прелоадера
│   │   │   ├── navigation.js     # Навигация + Scroll Spy
│   │   │   ├── animations.js     # Intersection Observer анимации
│   │   │   ├── sakura.js         # Canvas анимация лепестков
│   │   │   ├── faq.js            # FAQ аккордеон (900+ строк)
│   │   │   ├── carousel.js       # Карусель с drag/swipe
│   │   │   ├── gallery.js        # Lightbox с клавиатурой
│   │   │   └── forms.js          # Валидация и отправка форм
│   │   └── main.js               # 🎯 Главная точка входа
│   └── styles.css                # 🎯 Главный CSS агрегатор (@import)
├── dist/                         # Build артефакты (генерируется)
├── content/blog/                 # Markdown статьи (front‑matter)
├── index.html                    # Основная разметка
├── build.mjs                     # Build система (esbuild + PostCSS)
├── og.js                         # Генерация OG-изображения
├── manifest.json                 # PWA манифест
└── README.md                     # Документация

```

### 🎨 CSS Модули (16 файлов, 2,500+ строк)

**Base система:**
- `variables.css` (76 строк) — Design tokens, цвета, spacing, shadows
- `themes.css` (52 строки) — 4 сезонные темы (spring, autumn, winter, sakura)
- `reset.css` (69 строк) — Глобальные сбросы, smooth scroll
- `layout.css` (74 строки) — Container, skip-link, page transitions

**UI компоненты:**
- `preloader.css` (125 строк) — Загрузочный экран с анимированным кольцом
- `header.css` (173 строки) — Фиксированная шапка с blur, мобильное меню
- `hero.css` (208 строк) — Hero с градиентами и photo stack
- `faq.css` (438 строк) — Extreme minimalism FAQ аккордеон
- `buttons.css` (73 строки) — 5 вариантов кнопок
- `forms.css` (113 строк) — Поля форм, валидация, error states
- `sections.css` (305 строк) — About, Programs, Timeline, Gallery
- `reviews.css` (211 строк) — Карусель отзывов с touch support
- `modal.css` (100 строк) — Модальные окна с backdrop filter
- `gallery.css` (119 строк) — Lightbox с навигацией
- `footer.css` (121 строка) — Footer с social links
- `utilities.css` (180 строк) — Анимации, responsive helpers

### 🚀 JavaScript модули (11 файлов, ES6)

**Utilities:**
- `analytics.js` — Легковесная очередь событий аналитики
- `api.js` — Отправка на backend, offline queue, mock mode

**Компоненты:**
- `theme.js` — Переключение между 6 темами (dark, light, spring, autumn, winter, sakura)
- `preloader.js` — Fade-out прелоадера после загрузки
- `navigation.js` — Мобильное меню, smooth scroll, scroll spy, scroll progress
- `animations.js` — IntersectionObserver, count-up, parallax, spotlight cursor
- `sakura.js` — Canvas анимация падающих лепестков сакуры (200+ строк)
- `faq.js` — FAQ аккордеон с поиском, фильтрами, прогресс-баром, localStorage (480+ строк)
- `carousel.js` — Карусель отзывов с auto-play, drag/swipe, keyboard nav (330+ строк)
- `gallery.js` — Lightbox с клавиатурной навигацией и accessibility (160+ строк)
- `forms.js` — Валидация форм, modal управление, отправка данных (280+ строк)

**Main entry:**
- `main.js` — Импорт и инициализация всех модулей, responsive images

### 🌐 Билингвальные комментарии

Все модули содержат **полностью двуязычные комментарии** (EN/RU) для мультинациональных команд:

```javascript
/* =============================================
   Component Name
   Название компонента
   ============================================= */

/* EN: Description of what this does
   RU: Описание того, что это делает */
function example() {
  // ...
}
```

```css
/* =============================================
   Button Component
   Компонент кнопки
   ============================================= */

/* Primary button variant | Вариант основной кнопки */
.btn.primary {
  background: linear-gradient(135deg, var(--primary), var(--accent));
}
```

## Секции
1. Hero + call-to-action
2. О школе (ценности)
3. Основатель (био + метрики)
4. Достижения (таймлайн)
5. Программы (карточки с hover/tilt)
6. Методика (модули)
7. Галерея (грид + полноэкранный лайтбокс)
8. Отзывы (автокарусель + свайп + точки)
9. Контактная форма (валидация на клиенте)
10. Footer

## Визуальная концепция
- Тёмная тема по умолчанию + переключение на светлую (localStorage)
- Цвета: сакура (primary), золото (accent), глубокий тёмный фон
- Шрифты: Playfair Display (заголовки), Inter (текст)
- Градиенты + мягкие стеклянные панели (backdrop blur)
- Адаптив: брейкпоинт 860px + авто-fit гриды

## Анимации
- IntersectionObserver для постепенного появления (.fx-fade-up / .fx-scale-in)
- Canvas петали (рандомное движение + респаун, адаптация под моб.)
- Счётчики (Count-Up) метрик
- Tilt-параллакс (CSS transform hover)
- Модальное окно программ
- Preloader + глобальный прогресс скролла + прогресс чтения в постах
- Карусель отзывов (авто, пауза при hover/focus, drag/swipe, клавиатура)
- Лайтбокс галереи (ESC, стрелки, фокус-возврат)

## Доступность (A11y)
- aria-label / aria-labelledby где нужно
- Навигация с клавиатуры (focus-visible)
- prefers-reduced-motion поддержка (отключение сложных анимаций)
- Контраст > AA для ключевых элементов

## SEO / Структура
- Заголовки h1–h3 упорядочены
- meta description + keywords
- OpenGraph / Twitter
- JSON-LD (Organization + Founder Person + Article для постов)
- `sitemap.xml` (генерируется автоматически: главная, блог, посты, `changefreq` / `priority`)
- RSS (`/rss.xml`) и Atom (`/atom.xml`) фиды
- Canonical ссылки + feed discovery `<link rel="alternate" type="application/rss+xml">`
- `robots.txt` (авто: Allow: / + ссылка на sitemap)

## Сборка / Запуск
### Быстрый просмотр (без сборки)
Открыть `index.html` напрямую в браузере или поднять локальный сервер.

### Production build
```
npm install
npm run build
```
Результат в папке `dist/`:
- Хэшированные файлы: `styles.[hash].css`, `main.[hash].js`
- Обновлённый `service-worker.js` с новым VERSION и ссылками на хэш-ассеты
- Скопированы статические изображения, манифест, `fonts.css` + woff2 шрифты
- Сгенерированы `og-image.png` (если доступен `sharp`) и `og-image.svg`

### Режим разработки (watch + локальный сервер)
```
npm run dev
```
Сервер: http://localhost:5173 (без агрессивного кэша)

### Очистка
```
npm run clean
```

## Интеграция с backend (заглушка)
- До подключения сервера формы работают в демо-режиме: данные попадают в локальную очередь (`localStorage` ключ `japanschool.pendingForms`) и пользователю показывается успех.
- Перед подключением `main.js` можно определить глобальный объект с реальными эндпоинтами:
	```html
	<script>
		window.__API_ENDPOINTS = {
			lead: 'https://your-domain/api/leads',
			program: 'https://your-domain/api/program-interest'
		};
	</script>
	```
- Отправка: `POST`, `application/json`. Поля: `name`, `email`, `goal`, `level`, `message` (для lead), `program` (для модального окна) + автоматические `page`, `utm`, `timestamp`.
- При ошибке/отсутствии эндпоинта данные остаются в очереди и отмечаются как `mock`. После развёртывания сервера достаточно вернуть `200-204`.

## OG изображение
- Скрипт `og.js` создаёт `og-image.svg` и, при установленном `sharp`, `og-image.png`.
- Файлы генерируются на этапе `npm run build` и уже подключены в `<meta property="og:image">`.
- Если `sharp` установить не удалось, в `dist/og-image.png.note.txt` появится уведомление; соцсети используют svg.

## Темы и кастомизация
- Цвета: правьте CSS переменные в `:root`
- Скорость анимаций: переменные `--trans-fast`, `--trans-slow`
- Количество лепестков: константа `PETAL_COUNT` в `main.js`
- Сезонные темы: `data-theme="spring|autumn|winter|sakura"` (кнопка теперь циклически переключает пресеты)

## Responsive images
Скрипт сборки ищет `.jpg` в корне проекта и генерирует размеры (320,480,640,800,1024) в форматах JPG/WebP/AVIF.

Результат: `dist/img/` + манифест `img-manifest.json` (используется `main.js` для замены картинок галереи на `<picture>`). Если `sharp` не установлен – шаг пропускается.

Добавление нового изображения: поместите исходный `.jpg` рядом с `index.html` и выполните `npm run build`.

## Блог (Markdown)
Каждый файл `content/blog/*.md` должен начинаться с front‑matter:
```yaml
---
title: "Заголовок"
slug: optional-slug (если не указать – формируется из title)
date: 2025-09-01
description: "Краткое описание"
keywords: ["ключевые","слова"]
cover: optional-image.jpg
---

Текст **Markdown** с заголовками, списками и т.д.
```
При сборке генерируется:
- `dist/blog/<slug>/index.html`
- `dist/blog/index.html` – список постов
- JSON-LD (`Article`) + OG/Twitter метаданные (title/description/date + og:image при наличии cover)
- Индикатор чтения (если контент достаточно длинный; для коротких скрывается автоматически)
- Обёртка обложки в `<figure>`; если файл обложки есть среди сгенерированных responsive изображений — создаётся `<picture>` с AVIF/WebP/JPG источниками.

Cover обработка:
1. Положите исходный JPG (совпадающее имя) в корень проекта.
2. Сборка создаст варианты в `dist/img/` и запишет их в `img-manifest.json`.
3. Укажите `cover: original-name.jpg` во front‑matter — страница поста получит `<picture>` и `<meta property="og:image">`.

Twitter мета добавляется автоматически (summary_large_image) при наличии description.

### Canonical & Feeds
Для главной, индекса блога и каждой статьи автоматически добавляются:
```html
<link rel="canonical" href="https://example.com/blog/slug/">
<link rel="alternate" type="application/rss+xml" title="RSS" href="https://example.com/rss.xml">
<link rel="alternate" type="application/atom+xml" title="Atom" href="https://example.com/atom.xml">
```
При смене домена — обновите `site.config.json` и пересоберите.

### site.config.json
Файл конфигурации сайта:
```json
{
	"siteUrl": "https://example.com",
	"siteName": "Школа японского языка"
}
```
`siteUrl` используется для sitemap, canonical, RSS/Atom и og:url (позже можно расширить). Меняйте домен один раз здесь.

Override без редактирования файла (CI/CD):
1. Аргумент командной строки: `node build.mjs --siteUrl=https://prod.example.com`
2. Переменная окружения: `SITE_URL=https://prod.example.com npm run build`
Приоритет: ENV > CLI > файл.

## Добавление статьи – шаги
1. Создайте файл в `content/blog/` (пример: `my-post.md`).
2. Заполните front‑matter и контент.
3. Запустите `npm run build`.
4. Откройте `dist/blog/<slug>/`.

## Scroll & Reading Progress
- Глобальная полоса (`.scroll-progress`) – для всей страницы.
- Индикатор чтения поста – встроен инлайном в HTML поста (JS снизу страницы).

## Очередь форм и аналитика
Без изменений: механизм очереди и отправки событий совместим с добавлением блога.

## Обновлено
- Добавлены сезонные темы
- Responsive images pipeline
- Статический блог + индикатор прогресса чтения
- Замена галереи на `<picture>` через манифест
- JSON-LD Article + OG/Twitter meta для постов + dateModified
- og:url во всех страницах (главная, блог, посты)
- JSON-LD image (авто выбор: крупнейший responsive variant или cover, иначе og-image.png)
- robots.txt генерация
- CLI / ENV override для siteUrl
- Нормализация дат постов в ISO 8601 + предупреждения при отсутствии title/date
- Responsive cover `<picture>` + og:image
- Автоскрытие прогресс-бара для коротких постов
- Генерация RSS + Atom
- Генерация sitemap.xml (c changefreq/priority)
- Canonical + feed discovery ссылки
- Конфиг `site.config.json` для домена / имени

## Следующие шаги (если понадобится)
1. Подключение реального backend (использовать `window.__API_ENDPOINTS`)
2. Расширенная валидация (inline ошибки с aria-describedby)
3. Оптимизация изображений (AVIF / WebP, генерация разных размеров)
4. Дополнительный offline fallback (страница `/offline`)
5. Lighthouse тюнинг (critical CSS, дополнительные prefetch для галереи)

## Идеи для серверной части (позже)
- Backend: Node.js (NestJS) или Python (FastAPI)
- БД: PostgreSQL (основное), Redis (кеш, сессии)
- Auth: OAuth + Magic Links
- Хранилище медиа: S3/Wasabi
- Очереди: BullMQ / Celery (например генерация карточек)
- AI подсказки по грамматике / разбор ошибок

## Улучшения (возможные)
- Source Sans JP / Noto Sans JP fallback для японского
- Оптимизация изображений (WebP/AVIF версии)
- Ленивая подгрузка дополнительных секций
- Расширенный Service Worker (stale-while-revalidate для изображений)
- Lighthouse Performance тюнинг (critical CSS, prefetch)
 - Автогенерация списка файлов в SW (Workbox) вместо ручного патча
 - PurgeCSS для удаления неиспользуемых стилей
 - Split JS (инициализация / тяжёлые эффекты) + modulepreload

## Лицензия
Внутренний проект. Использование — по согласованию.
