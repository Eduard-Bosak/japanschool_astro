# Minimalist Reading Mode Update 🌸

## Обновление минималистичного режима чтения

**Дата:** 3 октября 2025  
**Версия:** 2.1 (Minimalist Edition)

---

## 📝 Изменения

### 1. Упрощённый режим чтения

#### Удалено:

- ❌ Кнопка **A++** (очень крупный шрифт)
- ❌ Текстовые метки секций ("Размер", "Вид", "Действия")
- ❌ Излишняя визуальная сложность

#### Обновлено:

- ✅ **8 кнопок** вместо 10 (минимализм)
- ✅ Более компактная панель управления (32x32px кнопки)
- ✅ Прозрачный фон с blur-эффектом
- ✅ Упрощённые тултипы (короткие подсказки)

#### Структура кнопок:

**Размер текста:**

- `A-` — Мельче
- `A+` — Крупнее

**Внешний вид:**

- `Aa` — Serif (шрифт с засечками)
- `⇔` — Шире (широкая колонка) **[ИСПРАВЛЕНО]**
- `◉` — Фокус (скрыть хедер/футер)
- `◐` — Контраст (высокий контраст)

**Действия:**

- `🖨` — Печать
- `🔗` — Поделиться
- `★` — Закладка

---

### 2. Исправление широкой колонки

**Проблема:** Режим `⇔` (wide) не работал — колонка не расширялась.

**Решение:**

```css
/* До */
body.reading-mode-wide .post-content {
  max-width: 1080px !important;
}

/* После */
body.reading-mode-wide main.container {
  max-width: 1080px !important;
}

body.reading-mode-wide .post-content {
  max-width: 100% !important;
}
```

Теперь кнопка `⇔` реально расширяет колонку с 820px до 1080px.

---

### 3. Падающая сакура 🌸

#### Оптимизация анимации:

- **Было:** 70 лепестков (desktop), 40 (mobile)
- **Стало:** 30 лепестков (desktop), 15 (mobile)
- **Скорость:** Уменьшена с 2.15 до 1.8 (более плавная)
- **Размер:** 5-15px вместо 6-20px (меньше, нежнее)
- **Прозрачность:** 0.3-0.6 вместо 0.4-0.9 (более тонкая)

#### Цветовая схема:

```javascript
// Градиент лепестков
g.addColorStop(0, 'rgba(255,255,255,0.6)'); // Белый центр
g.addColorStop(0.5, 'rgba(255,179,209,0.35)'); // Розовый
g.addColorStop(1, 'rgba(255,194,214,0)'); // Прозрачный край
```

#### SVG иконка сакуры:

Создан файл `src/assets/images/sakura-mini.svg` — минималистичный цветок для
использования в UI.

---

## 🎨 CSS Updates

### Новые стили (reading-mode.css):

```css
/* Minimalist controls */
.reading-controls {
  gap: 0.35rem; /* Меньше отступов */
  padding: 0.5rem 0.4rem; /* Компактнее */
  background: rgba(..., 0.6); /* Прозрачнее */
  backdrop-filter: blur(16px); /* Сильнее blur */
}

.reading-control-btn {
  width: 32px; /* Было: 38px */
  height: 32px;
  background: transparent; /* Было: rgba(primary, 0.1) */
  font-size: 0.95rem; /* Меньше */
}

/* Time estimate - minimalist */
.reading-time-estimate {
  padding: 0.5rem 0.85rem; /* Компактнее */
  font-size: 0.7rem; /* Меньше */
}
```

---

## 📱 Mobile Responsive

```css
@media (max-width: 900px) {
  .reading-controls {
    flex-direction: row; /* Горизонтальная панель */
    bottom: 5rem; /* Внизу экрана */
  }

  .reading-control-btn {
    width: 30px;
    height: 30px;
  }
}
```

---

## 🚀 Performance

### Сакура (Canvas):

- ✅ Автоматическая пауза при скрытии вкладки
- ✅ Отключение на `prefers-reduced-motion`
- ✅ Отключение на мобильных (< 768px)
- ✅ Frame throttling при скрытой вкладке

### Режим чтения:

- ✅ localStorage для сохранения настроек
- ✅ Взаимное исключение размеров (A-/A+)
- ✅ Tooltip только при hover (без JS)

---

## 📦 Build Output

```bash
[build] done: styles.b2c00e0fee.css main.1214b85637.js
```

**Новые хеши:**

- CSS: `b2c00e0fee`
- JS: `1214b85637`

---

## 🎯 Как использовать

### Запуск dev-сервера:

```bash
npm run dev
```

### Пересборка проекта:

```bash
npm run build
```

### Просмотр изменений:

1. Откройте `/blog/japanese-etiquette/`
2. Нажмите **Ctrl + F5** (hard reload)
3. Увидите:
   - 8 минималистичных кнопок справа
   - Падающую сакуру (на desktop)
   - Компактный time estimate внизу слева
   - Рабочую кнопку `⇔` (wide mode)

---

## ✨ Визуальные улучшения

### До:

- 10 кнопок с метками секций
- Яркие градиентные кнопки
- Крупные размеры (38px)
- 70 лепестков сакуры

### После:

- 8 кнопок без лишних меток
- Прозрачные минималистичные кнопки
- Компактные размеры (32px)
- 30 тонких лепестков сакуры
- Исправлен режим wide (⇔)

---

## 🐛 Исправленные баги

1. **Wide mode (⇔)** — теперь реально расширяет колонку
2. **JavaScript** — убрано `xlarge` из логики переключения размеров
3. **Сакура** — оптимизирована производительность (меньше лепестков, медленнее
   анимация)

---

## 📚 Связанные файлы

**Изменённые:**

- `build.mjs` — HTML template (убран A++, короткие тултипы)
- `src/styles/components/reading-mode.css` — минималистичные стили
- `src/scripts/components/sakura.js` — оптимизация (15-30 лепестков)

**Созданные:**

- `src/assets/images/sakura-mini.svg` — SVG иконка цветка
- `src/styles/components/sakura.css` — CSS для DOM-версии (не используется)
- `docs/MINIMALIST-UPDATE.md` — эта документация

---

## 🎨 Design Philosophy

> **Минимализм — это не отсутствие, это присутствие только необходимого.**

Новый дизайн следует японской эстетике **侘寂** (wabi-sabi):

- Простота форм
- Естественные цвета
- Прозрачность и лёгкость
- Функциональная красота

---

**Автор:** GitHub Copilot  
**Проект:** 日本語School Landing  
**Лицензия:** См. LICENSE
