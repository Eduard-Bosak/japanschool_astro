# FAQ Minimalist Redesign - 6 октября 2025

## Ультра минималистичный FAQ аккордеон

**Версия:** 2.2.0 (Minimalist FAQ)  
**Дата:** 6 октября 2025  
**Build:** `main.55d4b793c7.js` + `styles.8c6ad01e11.css`

---

## 🎯 Что изменилось

### Удалено (для простоты):

- ❌ Поиск по FAQ
- ❌ Фильтры по категориям
- ❌ Индикатор прогресса
- ❌ Кнопки "Развернуть всё" / "Свернуть всё"
- ❌ Заголовки групп
- ❌ Сохранение состояния в localStorage
- ❌ Левая акцентная полоса
- ❌ Квадратные границы карточек

### Добавлено (для красоты):

- ✅ Плавные cubic-bezier анимации (0.4, 0, 0.2, 1)
- ✅ Трансформация плюса в крестик при открытии
- ✅ Круглая иконка вместо квадратной
- ✅ Прозрачный фон с тонкими разделителями
- ✅ Увеличенные отступы (1.4rem вместо 0.8rem)
- ✅ Анимация появления контента (translateY + opacity)
- ✅ Hover эффекты с subtle подсветкой

---

## 🎨 Дизайн

### До (v2.1.1):

```css
.faq-item {
  background: #fff;
  border: 1px solid #ddd;
  border-radius: 4px;
  padding: 0;
}

.faq-trigger {
  padding: 0.7rem 0.8rem;
  font-size: 0.8rem;
}

.faq-icon {
  width: 18px;
  height: 18px;
  border-radius: 2px;
  background: #ddd;
}
```

### После (v2.2.0):

```css
.faq-item {
  background: transparent;
  border: none;
  border-bottom: 1px solid rgba(var(--ink-rgb), 0.08);
  padding: 0;
}

.faq-trigger {
  padding: 1.4rem 0;
  font-size: 1rem;
}

.faq-icon {
  width: 24px;
  height: 24px;
  border-radius: 50%; /* Круг */
  background: rgba(var(--ink-rgb), 0.08);
  transform: rotate(45deg); /* При открытии */
}
```

---

## ⚡ Анимации

### 1. **Иконка** (плюс → крестик)

```css
.faq-icon {
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.faq-item.open .faq-icon {
  background: var(--primary);
  transform: rotate(45deg); /* Плюс превращается в X */
}

.faq-icon::before,
.faq-icon::after {
  background: var(--ink-soft);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.faq-item.open .faq-icon::before,
.faq-item.open .faq-icon::after {
  background: #fff; /* Белые линии на цветном фоне */
}
```

### 2. **Панель ответа** (slide + fade)

```css
.faq-panel {
  max-height: 0;
  opacity: 0;
  transform: translateY(-8px);
  transition:
    max-height 0.5s cubic-bezier(0.4, 0, 0.2, 1),
    opacity 0.4s ease,
    transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.faq-item.open .faq-panel {
  opacity: 1;
  transform: translateY(0);
  max-height: var(--panel-max);
}
```

### 3. **Hover эффект**

```css
.faq-item:hover {
  background: rgba(var(--primary-rgb), 0.02);
}
```

---

## 📐 Сравнение размеров

| Элемент             | До (v2.1.1)     | После (v2.2.0)  | Изменение          |
| ------------------- | --------------- | --------------- | ------------------ |
| **Padding кнопки**  | 0.7rem 0.8rem   | 1.4rem 0        | +100% по вертикали |
| **Font size**       | 0.8rem          | 1rem            | +25%               |
| **Иконка**          | 18×18px квадрат | 24×24px круг    | +33%               |
| **Gap между items** | 0.6rem          | 0               | Убрано             |
| **Borders**         | 1px solid #ddd  | 1px bottom only | Минимализм         |

---

## 🚀 Производительность

### Удалённые функции (экономия JS):

**Было:**

```javascript
// 380+ строк кода
-applyFilter() -
  updateProgress() -
  persist() -
  restore() -
  expandAll() -
  collapseAll() -
  setupSearch() -
  highlight() -
  norm();
```

**Стало:**

```javascript
// 150 строк кода (экономия ~60%)
-toggle() - open() - close() - measure() - setupInteractions();
```

### Размер файлов:

| Файл    | v2.1.1  | v2.2.0     | Изменение |
| ------- | ------- | ---------- | --------- |
| **CSS** | 49.6 KB | **~45 KB** | -10%      |
| **JS**  | 27.7 KB | **~24 KB** | -13%      |

---

## 🎯 Визуальные изменения

### Структура FAQ (до):

```
┌─────────────────────────────┐
│ 🔍 Поиск...          [Найдено: 4] │
├─────────────────────────────┤
│ [Все] [JLPT] [Формат] [Поддержка] │
├─────────────────────────────┤
│ Развернуть всё | Свернуть всё │
├─────────────────────────────┤
│ ████░░░░░░  1/4 открыто     │
└─────────────────────────────┘

┌─ JLPT ──────────────────────┐
│ ┌─────────────────────────┐ │
│ │ Вопрос 1              [+]│ │
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │ Вопрос 2              [+]│ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

### Структура FAQ (после):

```
Вопрос 1                        ⊕
─────────────────────────────────
Вопрос 2                        ⊕
─────────────────────────────────
Вопрос 3                        ⊕
─────────────────────────────────
Вопрос 4                        ⊕
```

**Чистота и простота!** ✨

---

## 📱 Адаптивность

### Mobile (< 768px):

```css
@media (max-width: 768px) {
  .faq-trigger {
    font-size: 0.95rem;
    padding: 1.2rem 0;
  }

  .faq-icon {
    --sz: 20px;
  }

  .faq-panel-inner {
    font-size: 0.9rem;
    padding-bottom: 1.2rem;
  }
}
```

---

## ♿ Доступность (сохранена!)

- ✅ Клавиатурная навигация (Arrow Up/Down, Home/End)
- ✅ `aria-expanded`, `aria-controls`, `aria-labelledby`
- ✅ Focus outline (2px solid var(--primary))
- ✅ `prefers-reduced-motion: reduce` поддержка
- ✅ Semantic HTML (`<button>`, `role="region"`)

---

## 🎭 Демонстрация анимаций

### Последовательность открытия:

```
1. Click на кнопку
   ↓
2. requestAnimationFrame
   ↓
3. .faq-item.open добавлен
   ↓
4. Одновременно:
   - Иконка: rotate(0 → 45deg) + background (gray → primary)
   - Панель: max-height (0 → auto) + opacity (0 → 1) + translateY (-8px → 0)
   - Текст: color (ink → primary) + font-weight (500 → 600)
   ↓
5. Transition: 0.4-0.5s cubic-bezier
   ↓
6. Finish: transitionend event
```

---

## 🔧 Код изменений

### CSS (сокращённый):

**Удалено:**

- `.faq-controls` (display: none)
- `.faq-search-wrap` (display: none)
- `.faq-filters` (display: none)
- `.faq-bulk` (display: none)
- `.faq-progress` (display: none)
- `.faq-group-title` (display: none)
- `.faq-item::after` (левая полоса)

**Изменено:**

- `.faq-item`: transparent background, border-bottom only
- `.faq-trigger`: padding 1.4rem, font-size 1rem
- `.faq-icon`: 24px круг, rotate(45deg) при открытии
- `.faq-panel`: добавлен transform: translateY

### JS (сокращённый):

**Удалено:**

- `applyFilter()` - фильтрация и поиск
- `updateProgress()` - индикатор прогресса
- `persist()` / `restore()` - localStorage
- `expandAll()` / `collapseAll()` - массовые действия
- `setupSearch()` - поиск
- `handleDeepLink()` - прямые ссылки

**Сохранено:**

- `toggle()` / `open()` / `close()` - базовая логика
- `measure()` - расчёт высоты
- `setupInteractions()` - клавиатура + клики

---

## 📦 Build Output

```bash
npm run build

[images] generated responsive variants
[sitemap] sitemap.xml generated
[robots] robots.txt generated
[feed] rss.xml & atom.xml generated
[build] done: styles.8c6ad01e11.css main.55d4b793c7.js
```

**Новые хеши:**

- CSS: `8c6ad01e11` (минималистичные стили)
- JS: `55d4b793c7` (упрощённая логика)

---

## ✅ Тестирование

### Что проверить:

1. **Анимация иконки:**
   - Нажать на вопрос → плюс должен повернуться в крестик
   - Цвет фона: серый → primary
   - Линии: серые → белые

2. **Анимация панели:**
   - Панель появляется плавно (slide + fade)
   - Transition: 0.4-0.5s cubic-bezier
   - Текст должен быть читаемым сразу

3. **Hover эффекты:**
   - При наведении: subtle подсветка (primary 2%)
   - При открытии: более яркая подсветка (primary 3-5%)

4. **Клавиатура:**
   - Arrow Down/Up: навигация между вопросами
   - Enter/Space: открытие/закрытие
   - Home/End: первый/последний вопрос

5. **Mobile:**
   - Font size уменьшен до 0.95rem
   - Padding уменьшен до 1.2rem
   - Иконка 20px вместо 24px

---

## 🎨 Цветовая схема

### Dark theme:

```css
--ink: #e4e4e7 (текст вопроса) --ink-soft: #a1a1aa (текст ответа)
  --primary: #ff4d8f (акцент при открытии);
```

### Light theme:

```css
--ink: #18181b (текст вопроса) --ink-soft: #71717a (текст ответа)
  --primary: #ff4d8f (акцент при открытии);
```

---

## 📊 Сравнение версий

| Параметр         | v2.1.1 (Complex) | v2.2.0 (Minimalist)       |
| ---------------- | ---------------- | ------------------------- |
| **Строк CSS**    | ~350             | ~180 (-49%)               |
| **Строк JS**     | 380              | 150 (-61%)                |
| **Функций**      | 11               | 5 (-55%)                  |
| **UI элементов** | 10+              | 3 (вопрос, ответ, иконка) |
| **Анимаций**     | 2                | 5 (+150%)                 |
| **localStorage** | Да               | Нет                       |
| **Поиск**        | Да               | Нет                       |
| **Фильтры**      | Да               | Нет                       |

---

## 🚀 Миграция

### Если нужно вернуть функции:

1. **Поиск:** Раскомментировать `.faq-search-wrap` в CSS
2. **Фильтры:** Раскомментировать `.faq-filters` в CSS
3. **Прогресс:** Раскомментировать `.faq-progress` в CSS
4. **localStorage:** Восстановить `persist()` / `restore()` в JS

### Если нужно кастомизировать анимации:

```css
/* Быстрее */
.faq-panel {
  transition:
    max-height 0.3s ease,
    opacity 0.2s ease,
    transform 0.3s ease;
}

/* Медленнее */
.faq-panel {
  transition:
    max-height 0.8s cubic-bezier(0.4, 0, 0.2, 1),
    opacity 0.6s ease,
    transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Без анимации */
@media (prefers-reduced-motion: reduce) {
  .faq-panel,
  .faq-icon,
  .faq-trigger {
    transition: none;
  }
}
```

---

## 💡 Рекомендации

1. **Hard refresh** после обновления:

   ```
   Ctrl + F5 (Windows)
   Cmd + Shift + R (Mac)
   ```

2. **Проверить на мобильных:**
   - Размер иконки должен быть 20px
   - Padding кнопок уменьшен
   - Font size 0.95rem

3. **Протестировать темы:**
   - Dark: серая иконка → розовая при открытии
   - Light: серая иконка → розовая при открытии

---

**Отчёт завершён** ✅  
**FAQ стал простым, красивым и анимированным!** 🎨✨
