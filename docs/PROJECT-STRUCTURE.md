# 📁 Project Structure Guide

## Правильная организация файлов проекта

### ✅ Корневая директория (Root)

**Что ДОЛЖНО быть в корне:**

```
japanschool/
├── .editorconfig           # IDE настройки форматирования
├── .eslintignore          # Исключения для ESLint
├── .eslintrc.json         # Конфигурация ESLint
├── .gitignore             # Исключения для Git
├── .prettierignore        # Исключения для Prettier
├── .prettierrc.json       # Конфигурация Prettier
├── build.mjs              # Главный build script
├── og.js                  # OG image generator script
├── postcss.config.cjs     # PostCSS конфигурация
├── lighthouserc.json      # Lighthouse CI конфигурация
├── site.config.json       # Настройки сайта
├── package.json           # npm dependencies
├── package-lock.json      # npm lock file
├── README.md              # Главная документация
├── CHANGELOG.md           # История изменений
└── LICENSE                # Лицензия проекта
```

**Что НЕ должно быть в корне:**

❌ `index.html`, `main.js`, `styles.css` - артефакты сборки (генерируются в
`dist/`) ❌ `service-worker.js`, `manifest.json`, `offline.html`,
`favicon.svg` - исходники в `public/` ❌ `*.md` файлы кроме
README/CHANGELOG/LICENSE - должны быть в `docs/` ❌ HTML шаблоны - должны быть в
`content/templates/`

---

## 📂 Структура директорий

### `/src` — Исходный код

```
src/
├── scripts/              # JavaScript модули
│   ├── components/       # UI компоненты
│   ├── utils/           # Утилиты
│   └── main.js          # Главный entry point
├── styles/              # CSS модули
│   ├── base/           # Базовые стили
│   ├── components/     # Стили компонентов
│   └── styles.css      # Главный entry point
└── assets/             # Медиа ресурсы
    └── images/         # Изображения
```

### `/public` — Статические файлы

```
public/
├── index.html          # Главная HTML страница
├── offline.html        # Offline страница для PWA
├── manifest.json       # PWA manifest
├── service-worker.js   # Service Worker
└── favicon.svg         # Фавикон
```

### `/content` — Контент сайта

```
content/
├── blog/              # Markdown посты блога
│   ├── example.md
│   └── ...
└── templates/         # HTML шаблоны
    └── blog-template.html
```

### `/docs` — Документация

```
docs/
├── AUDIT-REPORT.md           # Аудит проекта
├── CODE-QUALITY-GUIDE.md     # Руководство по качеству кода
├── MIGRATION-GUIDE.md        # Гайд по миграции
├── PROJECT-STRUCTURE.md      # Этот файл
└── ...
```

### `/dist` — Build output (игнорируется в git)

```
dist/
├── index.html                # Оптимизированный HTML
├── styles.[hash].css         # Минифицированный CSS
├── main.[hash].js            # Минифицированный JS
├── service-worker.js         # Service Worker
├── manifest.json             # PWA manifest
├── img/                      # Оптимизированные изображения
│   ├── *.jpg
│   ├── *.webp
│   └── *.avif
├── sitemap.xml              # Карта сайта
├── robots.txt               # Правила для роботов
├── rss.xml                  # RSS лента
└── atom.xml                 # Atom лента
```

### `/.github` — GitHub конфигурация

```
.github/
└── workflows/
    ├── build.yml         # Build workflow
    └── lighthouse.yml    # Lighthouse CI workflow
```

---

## 🔄 Build Process Flow

```
1. Исходники (src/) → 2. Сборка (build.mjs) → 3. Артефакты (dist/)
                            ↓
                       public/ файлы
                       копируются в dist/
```

---

## 🚫 Файлы в .gitignore

**Всегда игнорировать:**

- `node_modules/` - npm зависимости
- `dist/` - результат сборки
- `.lighthouseci/` - отчёты Lighthouse
- `*.log` - лог файлы
- `.env*` - переменные окружения
- `.DS_Store`, `Thumbs.db` - системные файлы

**Игнорировать артефакты в корне:**

- `/index.html`, `/main.js`, `/styles.css` - должны быть только в `dist/`
- `/service-worker.js`, `/manifest.json`, `/offline.html` - должны быть в
  `public/`

---

## ✅ Checklist при добавлении новых файлов

- [ ] Файл в правильной директории?
- [ ] Не дублирует ли существующий файл?
- [ ] Добавлен ли в `.gitignore` если это артефакт?
- [ ] Есть ли комментарии/документация если нужно?
- [ ] Следует ли naming convention проекта?

---

## 📝 Naming Conventions

**JavaScript:**

- Файлы: `kebab-case.js` (navigation.js, api.js)
- Функции: `camelCase` (openModal, fetchData)
- Классы: `PascalCase` (BlogPost, ImageGallery)
- Константы: `SCREAMING_SNAKE_CASE` (API_TIMEOUT, STORAGE_KEY)

**CSS:**

- Файлы: `kebab-case.css` (buttons.css, hero.css)
- Классы: `kebab-case` (.btn-primary, .hero-section)
- BEM модификаторы: `--modifier` (.btn--large, .card--featured)

**Markdown:**

- Файлы: `SCREAMING-KEBAB-CASE.md` (README.md, CHANGELOG.md)
- Исключение: посты блога `kebab-case.md` (example-post.md)

---

## 🔧 Maintenance

**Регулярно проверять:**

1. Нет ли лишних файлов в корне: `ls -la`
2. Актуален ли `.gitignore`: `git status`
3. Соответствует ли структура этому документу

**При refactoring:**

1. Обновить этот документ
2. Обновить `README.md` если нужно
3. Проверить `build.mjs` на правильность путей

---

**Last updated:** 2025-01-11 **Version:** 1.1.0
