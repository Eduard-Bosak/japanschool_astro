# Quick Commands Cheat Sheet

# Шпаргалка быстрых команд

## 🚀 Самые используемые

### Запустить проект

```bash
npm run dev:stable
```

Затем открой: **http://localhost:5173** (или порт, который покажет в консоли)

### Проверить перед коммитом

```bash
npm run lint
npm run format
npm run build
```

---

## 📦 Сборка

```bash
npm run build         # Production (минификация, оптимизация)
npm run build:fast    # Development (быстрее, без минификации)
npm run clean         # Полная очистка dist
```

---

## 🔧 Разработка

```bash
npm run dev           # Build + Watch + Сервер (с логами)
npm run dev:stable    # Build + Watch + Сервер (с Live Reload!)
npm run watch         # Только отслеживание изменений
npm run serve         # Только сервер (с авто-выбором порта)
npm run preview       # Тихий сервер для предпросмотра
```

**✨ Live Reload:** При `npm run dev:stable` браузер автоматически обновляется
при изменении файлов!

---

## ✨ Качество кода

```bash
npm run lint          # Проверка ESLint
npm run lint:fix      # Исправить автоматически
npm run format        # Форматирование Prettier
npm run format:check  # Проверка форматирования
```

---

## 🚀 Деплой

```bash
npm run deploy:check  # Полная предеплойная проверка (lint + format + build + preview)
npm run deploy        # Production build + инструкции по деплою
```

**CI/CD:** Push в main → автоматический деплой на GitHub Pages (см.
docs/CICD-GUIDE.md)

---

## 🎯 Performance

```bash
npm run lighthouse    # Lighthouse аудит производительности
```

---

## 🔍 Диагностика

### Проверить что dist собран

```bash
ls dist/index.html
```

### Найти процесс на порту (Windows)

```bash
netstat -ano | findstr :5173
taskkill /PID <номер> /F
```

### Найти процесс на порту (Linux/Mac)

```bash
lsof -i :5173
kill -9 <PID>
```

### Полная переустановка зависимостей

```bash
rm -rf node_modules package-lock.json
npm install
```

---

## 🎨 Стили и темы

Файлы тем находятся в:

- `src/styles/base/themes.css` — определения тем
- `src/styles/base/variables.css` — CSS переменные

---

## 📝 Блог

Создать новую статью:

1. Создай файл `content/blog/my-article.md`
2. Добавь frontmatter:

```yaml
---
title: 'Заголовок статьи'
date: '2025-10-15'
description: 'Краткое описание'
cover: 'image.jpg'
keywords: 'japanese, jlpt, learning'
categories: ['JLPT', 'Методика']
---
```

3. Напиши содержимое в Markdown
4. Запусти `npm run dev:stable` — статья автоматически появится

---

## 🌐 URLs после запуска

- **Главная:** http://localhost:5173
- **Блог:** http://localhost:5173/blog/
- **Статья:** http://localhost:5173/blog/slug/
- **RSS:** http://localhost:5173/rss.xml
- **Atom:** http://localhost:5173/atom.xml
- **Sitemap:** http://localhost:5173/sitemap.xml
- **Offline:** http://localhost:5173/offline.html

---

## 🔥 Горячие клавиши

Во время работы dev-сервера:

- **Ctrl+C** — остановить сервер
- **Ctrl+Shift+R** (браузер) — жёсткая перезагрузка (очистить кэш)
- **F12** (браузер) — DevTools

---

## 📚 Дополнительная документация

- **DEV-SETUP.md** — полная документация по настройке разработки
- **PROJECT-STRUCTURE.md** — структура проекта
- **READING-MODE-GUIDE.md** — режим чтения для блога
- **CODE-QUALITY-GUIDE.md** — стандарты кода

---

## ⚡ Pro Tips

1. **Используй `dev:stable`** вместо `dev` — меньше шума в консоли
2. **Проверяй lighthouse перед деплоем** — убедись что performance >90
3. **Коммить только src/ и content/** — dist генерируется автоматически
4. **Чистый коммит:**
   ```bash
   npm run lint:fix
   npm run format
   npm run build
   git add .
   git commit -m "feat: добавил новую статью"
   ```
5. **Порт занят?** — не переживай, сервер автоматически выберет 5174, 5175...

---

## 🐛 Частые проблемы

### "Directory listing" вместо сайта

✅ Решено автоматически в новой версии! Если всё ещё видишь:

```bash
npm run clean
npm run dev:stable
```

### "EADDRINUSE: port already in use"

✅ Решено автоматически! `serve.mjs` выберет свободный порт.

### Watch не видит изменения

```bash
npm install  # переустановить chokidar
```

### Старые CSS/JS не обновляются в браузере

```bash
Ctrl+Shift+R  # жёсткая перезагрузка в браузере
```

---

**Готово к работе!** 🎉

Если нужна помощь — смотри DEV-SETUP.md или пиши мне.
