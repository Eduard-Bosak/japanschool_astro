# Режим чтения — Полный гайд (v2.0)

## Что добавлено (октябрь 2025)

### 1. **Полный Header с навигацией**

- Логотип "日本語School" ведет на главную
- Меню: Главная, О школе, Программы, Блог, Запись
- Переключатель темы (светлая/темная)
- Мобильное меню-гамбургер
- Хлебные крошки: Главная / Блог / Статья

### 2. **Расширенная панель управления** (правая часть)

Теперь **10 кнопок в 3 секциях**:

#### 📏 Секция "Размер":

1. **A-** — Уменьшить текст (0.9rem)
2. **A+** — Увеличить текст (1.15rem)
3. **A++** — Очень крупный текст (1.3rem)
   - Взаимоисключающие (только одна активна)

#### 🎨 Секция "Вид":

4. **Aa** — Шрифт с засечками (serif Georgia)
5. **⇔** — Широкая колонка (1080px вместо 820px)
6. **◉** — Режим фокуса (header/footer затемняются до 15%, восстанавливаются при
   наведении)
7. **◐** — Высокий контраст (черный фон #000, белый текст #fff)

#### ⚡ Секция "Действия":

8. **🖨** — Печать статьи (window.print)
9. **🔗** — Поделиться (Web Share API или копирование в буфер)
10. **★** — Закладка (сохранение в localStorage)

### 3. **Индикатор времени чтения** (левый нижний угол)

- Показывает оставшееся время: "Осталось читать: ~3 мин"
- Динамически обновляется при прокрутке
- Скрывается когда статья прочитана (>98%) или очень короткая
- Плавная анимация появления/исчезновения

### 4. **Улучшенная типографика** (rich-text.css)

- Градиентные заголовки H1
- Стильные таблицы с hover-эффектами
- Красивые цитаты с тенями
- Код-блоки с syntax highlighting готовностью
- Адаптивные изображения с border-radius
- Печатные стили для принтеров (11pt шрифт, черно-белая палитра)

### 5. **SEO и доступность для Google**

#### Schema.org разметка:

```json
{
  "@type": "Article",
  "headline": "Название статьи",
  "datePublished": "2024-09-15T00:00:00.000Z",
  "wordCount": 598,
  "timeRequired": "PT4M",
  "author": { "@type": "Organization", "name": "Школа японского языка" }
}
```

#### Breadcrumbs structured data:

```json
{
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "position": 1, "name": "Главная", "item": "https://example.com/" },
    { "position": 2, "name": "Блог", "item": "https://example.com/blog/" },
    {
      "position": 3,
      "name": "Статья",
      "item": "https://example.com/blog/slug/"
    }
  ]
}
```

#### ARIA-метки:

- `aria-label` на всех кнопках управления
- `aria-expanded` на мобильном меню
- `aria-labelledby` на секциях
- `role="navigation"` на меню

## Как использовать

### Для пользователя:

1. Открой любую статью блога
2. Справа увидишь панель с 3 секциями кнопок
3. **Размер**: выбери A-, A+ или A++ (только одна)
4. **Вид**: комбинируй любые (serif + wide + focus + contrast)
5. **Действия**:
   - 🖨 печать откроет диалог печати
   - 🔗 поделится (или скопирует ссылку)
   - ★ добавит в закладки
6. Настройки сохраняются автоматически между сессиями

### Прогресс чтения:

- **Верхняя полоска** показывает % прочитанного
- **Левый нижний угол** показывает оставшееся время

## Технические детали

### Файлы проекта:

- **src/styles/components/reading-mode.css** — стили панели, режимы, индикатор
  времени
- **src/styles/components/rich-text.css** — типографика контента (h1-h6, tables,
  quotes, code)
- **src/scripts/blog-post.ts** — вся логика (прогресс, TOC, режимы, share,
  bookmarks, mobile nav)
- **build.mjs** (line 375) — шаблон генерации HTML с полным header/footer

### CSS классы на body:

```css
.reading-mode-small   /* A- */
.reading-mode-large   /* A+ */
.reading-mode-xlarge  /* A++ */
.reading-mode-serif   /* Aa */
.reading-mode-wide    /* ⇔ */
.reading-mode-focus   /* ◉ */
.reading-mode-contrast /* ◐ */
```

### LocalStorage ключи:

- `readingModes` — массив активных режимов: `["serif", "large", "focus"]`
- `blogBookmarks` — массив путей закладок: `["/blog/slug1/", "/blog/slug2/"]`

### JavaScript API:

```javascript
// Получить все закладки
JSON.parse(localStorage.getItem('blogBookmarks') || '[]');

// Получить активные режимы
JSON.parse(localStorage.getItem('readingModes') || '[]');

// Добавить режим программно
document.body.classList.add('reading-mode-focus');
```

## Адаптивность

### Desktop (>900px):

- Панель справа (fixed, center vertical)
- Tooltips слева от кнопок
- Индикатор времени слева внизу
- Все секции вертикальные

### Mobile (≤900px):

- Панель справа внизу (над footer)
- Секции горизонтальные
- Tooltips скрыты
- Индикатор времени сжат
- Мобильное меню с гамбургером

### Tablet (768px-900px):

- Промежуточное состояние
- H1: 2rem, H2: 1.5rem
- Таблицы: 0.85rem
- Padding: 1rem вместо 1.5rem

### Print:

- Панель скрыта
- Шрифт 11pt
- Черно-белая палитра
- `page-break-inside: avoid` на pre/blockquote/table

## Google индексация

### Факторы ранжирования:

✅ **Core Web Vitals**:

- LCP < 2.5s (критические стили inline)
- FID < 100ms (defer на JS)
- CLS = 0 (fixed dimensions)

✅ **Mobile-friendly**:

- `meta viewport` настроен
- Touch targets ≥48px (кнопки 36-38px)
- Responsive typography

✅ **Structured Data**:

- Article schema с wordCount, timeRequired
- Breadcrumbs для навигации
- Organization для автора

✅ **Accessibility**:

- ARIA labels на всех интерактивных элементах
- Keyboard navigation (Tab, Enter, Space)
- Contrast ratio ≥4.5:1 (WCAG AA)

### Как Google видит режим чтения:

1. **Индексирует весь текст** (режимы не влияют на crawling)
2. **Распознает структуру** (h1-h6 hierarchy)
3. **Учитывает время чтения** (timeRequired в schema)
4. **Видит breadcrumbs** (внутренняя перелинковка)

## Примеры комбинаций

### 📖 "Книжный режим":

- Aa (serif) + ⇔ (wide) + A+ (large)
- Эффект: Georgia 1.15rem на 1080px колонке

### 🌙 "Ночное чтение":

- ◐ (contrast) + A++ (xlarge) + ◉ (focus)
- Эффект: Черный фон, белый текст 1.3rem, скрыты отвлекающие элементы

### 🔍 "Студенческий":

- A- (small) + Default (sans) + Default (820px)
- Эффект: Компактный текст, больше контента на экране

### 👴 "Для пожилых":

- A++ (xlarge) + ◐ (contrast) + ⇔ (wide)
- Эффект: Очень крупный контрастный текст

## Будущие улучшения (Roadmap)

- [ ] Дислексия-френдли шрифт (OpenDyslexic)
- [ ] Цветовые фильтры (сепия, синий свет)
- [ ] TTS (Text-to-Speech) озвучивание
- [ ] Прогресс по главам (chapter markers)
- [ ] Экспорт в PDF/ePub
- [ ] Синхронизация закладок между устройствами
- [ ] Комментарии к выделенным фрагментам
- [ ] Ночной режим по расписанию (auto dark 22:00-06:00)

---

**Создано:** 3 октября 2025 **Версия:** 2.0.0 **Авторы:** Eduard Bosak + GitHub
Copilot
