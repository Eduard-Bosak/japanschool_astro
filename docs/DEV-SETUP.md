# Development Setup Guide

# Руководство по настройке разработки

## Quick Start | Быстрый старт

### Stable Development Mode | Стабильный режим разработки

```bash
npm run dev:stable
```

**Что делает:**

- ✅ Полная production сборка
- ✅ Watch режим (автоматическая пересборка при изменениях)
- ✅ Локальный сервер с **автоматическим выбором свободного порта** (5173+)
- ✅ Тихий режим (без лишних логов сервера)

### Standard Development Mode | Обычный режим разработки

```bash
npm run dev
```

**Отличия от stable:**

- Полный вывод логов сервера
- Те же функции автоматического выбора порта

### Preview Mode | Режим предпросмотра

```bash
npm run build
npm run preview
```

Просто сервер без watch — для быстрого просмотра результата сборки.

---

## Problem Solved | Решённая проблема

### До улучшений:

❌ При запуске `npm run dev` иногда показывался **directory listing** вместо
главной страницы ❌ Причина: `build.mjs` удалял папку `dist` перед новой сборкой
в watch режиме → http-server видел пустую папку ❌ При повторном запуске падало
с `EADDRINUSE` если порт 5173 был занят

### После улучшений:

✅ **Безопасная сборка без удаления dist** в режиме watch ✅ **Автоматическая
очистка старых хешированных бандлов** (styles._.css, main._.js) ✅
**Автоматический выбор свободного порта** через `serve.mjs` (5173, 5174,
5175...) ✅ **Очередь сборки** — изменения не запускают несколько параллельных
сборок ✅ Добавлен **chokidar** в devDependencies для стабильного watch на всех
платформах

---

## Available Scripts | Доступные команды

### Build | Сборка

```bash
npm run build          # Production сборка (минификация, оптимизация)
npm run build:fast     # Dev сборка (без минификации, быстрее)
npm run clean          # Очистка dist полностью
```

### Development | Разработка

```bash
npm run dev            # Стандартный режим: build + watch + serve
npm run dev:stable     # Стабильный режим: build + watch + serve (silent)
npm run watch          # Только watch (без сборки и сервера)
npm run serve          # Только сервер (с автовыбором порта)
npm run serve:silent   # Только сервер (тихий режим)
npm run preview        # Alias для serve:silent
```

### Code Quality | Качество кода

```bash
npm run lint           # Проверка кода (ESLint)
npm run lint:fix       # Исправление линт-ошибок
npm run format         # Форматирование (Prettier)
npm run format:check   # Проверка форматирования
```

### Performance | Производительность

```bash
npm run lighthouse     # Lighthouse аудит
```

---

## Technical Details | Технические детали

### Новая архитектура сборки

**serve.mjs** — умный запуск сервера:

1. Проверяет доступность порта 5173
2. Если занят — автоматически пробует 5174, 5175... (до 10 попыток)
3. Запускает http-server на найденном порту
4. Поддерживает graceful shutdown (Ctrl+C)

**build.mjs** — безопасная сборка:

1. **Первая сборка**: полная очистка dist + build
2. **Watch режим**: НЕ удаляет dist, только обновляет файлы
3. **Очистка старых хешей**: после каждой сборки удаляет устаревшие
   `styles.*.css`, `main.*.js`, `blog-post.*.js`
4. **Очередь сборки**: если файл изменился во время сборки, запускается одна
   дополнительная сборка после завершения текущей (не параллельно)

### Watch Paths | Отслеживаемые пути

```javascript
[
  'public/index.html',
  'src/styles.css',
  'src/scripts', // вся папка
  'public/service-worker.js',
  'public/offline.html',
  'public/manifest.json',
  'content/blog' // блог (markdown)
];
```

---

## Troubleshooting | Устранение проблем

### Порт 5173 занят, но сервер не стартует

```bash
# Windows:
netstat -ano | findstr :5173
taskkill /PID <pid> /F

# Linux/Mac:
lsof -i :5173
kill -9 <pid>
```

**Но теперь это не требуется!** `serve.mjs` автоматически выберет свободный
порт.

### Directory listing вместо главной страницы

✅ **Исправлено автоматически**. Если всё ещё видишь — убедись что:

```bash
# Проверь наличие index.html
ls dist/index.html

# Если нет — запусти полную пересборку
npm run clean
npm run build
npm run dev:stable
```

### Watch не видит изменения

```bash
# Переустанови chokidar
npm install

# Проверь что файл в отслеживаемом списке (см. Watch Paths выше)
```

### Старые хеши не удаляются

✅ **Исправлено**. Функция `pruneOldHashed` автоматически чистит после каждой
сборки.

---

## Best Practices | Рекомендации

1. **Используй `dev:stable`** для ежедневной разработки
2. **`build` перед коммитом** — убедись что production сборка работает
3. **`lighthouse` перед деплоем** — проверь производительность
4. **Не коммить `dist`** — это генерируемая папка (должна быть в `.gitignore`)

---

## Port Auto-Selection Example | Пример автовыбора порта

```
Port 5173 is available:
🚀 Starting server on http://localhost:5173

Port 5173 is busy:
⚠️  Port 5173 is busy, using port 5174 instead
🚀 Starting server on http://localhost:5174
```

---

## Next Steps | Следующие шаги

Если нужны дополнительные улучшения:

- [ ] Live reload (автоматическое обновление страницы при изменениях)
- [ ] HMR для CSS (без перезагрузки страницы)
- [ ] Атомарная сборка через tmp папку
- [ ] Встроенный express сервер с middleware
- [ ] Source map manifest для анализа бандлов

Скажи если что-то из этого нужно — реализую!
