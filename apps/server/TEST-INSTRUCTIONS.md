# ✅ Backend запущен и готов к работе!

## 🎯 Текущий статус:

- ✅ **Backend сервер:** Работает на `http://localhost:3000`
- ✅ **Email настроен:** eduardbosak@gmail.com
- ✅ **Отправка email:** Активирована
- ✅ **Порт 3000:** Слушает соединения
- ✅ **Frontend:** Работает на `http://localhost:5173`

---

## 🧪 Как протестировать:

### Вариант 1: Тестовая страница (уже открыта в браузере)

Файл `server/test.html` уже открыт. **Обновите страницу (F5)** и нажмите кнопки:

1. **Test /health** - проверка здоровья сервера
2. **Test /api/submit-form** - тестовая отправка формы
3. **Test /api/submissions** - просмотр всех заявок

---

### Вариант 2: Реальная форма на сайте

1. Откройте `http://localhost:5173` в браузере
2. Прокрутите к секции с формой (Hero или Contacts)
3. Заполните форму:
   - **Имя:** Ваше имя
   - **Email:** Ваш email
   - **Телефон:** +79991234567
   - **Уровень:** N5 / N4 / N3 (выберите любой)
   - **Сообщение:** Тестовое сообщение
4. Нажмите "Отправить заявку"

---

## 📧 Что произойдет:

1. **Данные сохранятся** в `server/submissions.json`
2. **Email придет вам** на eduardbosak@gmail.com:
   - ✉️ Уведомление администратору (вам) с деталями заявки
   - ✉️ Авто-ответ пользователю (если email заполнен)

---

## 🔍 Проверка результатов:

### 1. Просмотр сохраненных заявок:

```powershell
Get-Content q:\japanschool\server\submissions.json | ConvertFrom-Json | ConvertTo-Json -Depth 10
```

### 2. Проверка через API:

Откройте в браузере: `http://localhost:3000/api/submissions`

### 3. Проверка email:

Зайдите на https://mail.google.com и проверьте входящие письма

---

## ⚠️ Важно про Gmail App Password:

Gmail не принимает обычные пароли для SMTP. Нужен **App Password**.

### Если email не отправляется:

1. Откройте: https://myaccount.google.com/security
2. Включите **двухфакторную аутентификацию** (2FA)
3. Перейдите: https://myaccount.google.com/apppasswords
4. Создайте новый App Password:
   - Приложение: **Mail**
   - Устройство: **Other (Custom name)** → введите "Japan School"
5. Скопируйте **16-значный пароль** (формат: `xxxx-xxxx-xxxx-xxxx`)
6. Замените в `server/.env`:
   ```env
   EMAIL_PASS=xxxx-xxxx-xxxx-xxxx
   ```
7. Перезапустите backend

---

## 🐛 Troubleshooting:

### Backend не отвечает:

```powershell
# Проверка порта
netstat -ano | findstr ":3000"

# Перезапуск
Get-Process node | Stop-Process -Force
node q:\japanschool\server\index.js
```

### CORS ошибка:

Убедитесь что:

- Frontend на `http://localhost:5173`
- Backend на `http://localhost:3000`
- В консоли браузера нет ошибок CORS

### Email не отправляется:

1. Проверьте `server/.env` - правильный ли email и пароль
2. Используйте **App Password**, не обычный пароль
3. Проверьте консоль backend на ошибки

---

## 📊 Логи backend:

Чтобы увидеть логи в реальном времени, откройте терминал где запущен backend.

Вы увидите:

- ✅ Успешные запросы
- ❌ Ошибки валидации
- 📧 Статус отправки email

---

## 🎉 Готово!

**Обновите страницу test.html в браузере и нажмите кнопки для проверки!**

Или откройте реальную форму на `http://localhost:5173`
